.origin
  opactiy 0
  transform scale(0)
  transition all .4s cubic-bezier(.75, -.5, 0, 1.4)
  text-transform none

.active
  .origin
    opacity 1
    transform scale(1)

  &.beforeEnd
    .origin
      opacity 0
      transform scale(0)
